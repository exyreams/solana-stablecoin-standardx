{
  "name": "@stbr/sss-token-sdk",
  "version": "0.1.0",
  "description": "Solana Stablecoin Standard — TypeScript SDK",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "cp ../../target/idl/*.json src/idl/ && tsc && mkdir -p dist/idl && cp src/idl/*.json dist/idl/",
    "test": "ts-mocha -p tsconfig.json -t 60000 tests/**/*.ts",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/spl-token": "^0.4.9",
    "@solana/web3.js": "^1.95.3"
  },
  "devDependencies": {
    "@types/chai": "^4.3.12",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.5",
    "chai": "^4.4.1",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.3.3"
  }
}